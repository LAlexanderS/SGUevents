---
title: Руководство для администраторов
---

{{ hero("Администраторам", "контроль качества и инфраструктуры") }}

## Зоны ответственности

- Управление справочниками (кафедры, площадки, типы событий).
- Модерация контента и заявок на организаторов.
- Настройка интеграций (SSO, Telegram, webhooks).
- Observability: метрики, логи, очереди задач.

## Панели управления

| Панель                | Что содержит                                                |
| --------------------- | ----------------------------------------------------------- |
| Django Admin          | Базовые модели, справочники, доступы                        |
| «Мониторинг»          | Celery, очереди, отложенные задачи                          |
| «Интеграции»          | Ключи API, проверка подключения, тестовые webhooks          |
| «Контент»             | Модерация событий, массовые правки                          |

## Режимы доступа

1. **Admin light** — работа со справочниками, без системных настроек.
2. **Super admin** — полный доступ, изменения инфраструктуры.
3. **Readonly auditor** — просмотр логов и отчётов без права записи.

## Чек-лист перед релизом

- [ ] `python manage.py check --deploy`
- [ ] `mkdocs build` и публикация свежей документации.
- [ ] Обновление переменных окружения (`SITE_DOMAIN`, `TG_BOT_URL`, `SUPPORT_EMAIL`).
- [ ] smoke-тесты бота (`/health`, `/events`).

## Observability

!!! info "Метрики"
    - RPS веб-приложения  
    - Время ответа API бота  
    - Очередь Celery (age, length)  
    - Ошибки авторизации/SSO

!!! danger "Инциденты"
    Используйте шаблон постмортема: событие → влияние → корень проблемы → действия.

## Полезные команды

```bash
python manage.py clearsessions
python manage.py collectstatic --noinput
celery -A SGUevents worker -l info
mkdocs serve --dev-addr localhost:8088
```

## Контакты

- Инфраструктурная поддержка: {{ SUPPORT_EMAIL }}
- Чат админов в Telegram: запрос через куратора безопасности.

